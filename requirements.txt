# TerraFM LULC Segmentation – Python dependencies
# Install with: pip install -r requirements.txt
#
# Pinned to versions tested on Google Colab (T4, Python 3.10)
# Colab already provides: torch, torchvision, numpy, matplotlib

# ---- Core deep learning ----
torch>=2.1.0
torchvision>=0.16.0

# ---- TerraFM loading ----
transformers>=4.40.0
huggingface_hub>=0.22.0
timm>=0.9.12              # ViT backbone fallback + feature extraction

# ---- Geospatial / TIFF ----
rasterio>=1.3.9
GDAL>=3.6.0               # rasterio dependency (usually auto-installed)

# ---- Data science ----
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ---- Visualization ----
matplotlib>=3.7.0
seaborn>=0.12.0

# ---- Progress & utilities ----
tqdm>=4.66.0

# ---- Optional: LoRA support (Stage 3 fine-tuning) ----
# peft>=0.10.0            # Uncomment if using LoRA adapters

# ---- Development ----
scikit-learn>=1.3.0       # used for GroupKFold fallback in prepare_dataset.py
